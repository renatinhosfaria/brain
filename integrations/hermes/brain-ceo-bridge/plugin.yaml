name: brain-ceo-bridge
version: 1.0.0
description: "Expose trusted WhatsApp transport context for the current DM contact to the CEO through the local Brain gateway."
author: Brain maintainers
provides_tools:
  - conversation_context
requires_env:
  - BRAIN_GATEWAY_TOKEN
